# fff - Codebase Index
# Generated: 2026-05-31T02:44:48.851Z via deepseek-chat

project:
  name: fff
  version: 1.0.0
  description: "fff (fucking fucking fast) — a fast terminal AI coding assistant"
  language: TypeScript
  runtime: Bun

files:
  - path: codebase-index.yaml
    description: "Provides a structured index of the project's metadata, entry points, and source files for the AI assistant."
  - path: install.sh
    description: "Automates the installation of the fff tool by downloading and setting up dependencies like Bun."
  - path: package.json
    description: "Defines the fff project's metadata, dependencies, scripts, and entry point for Node.js/Bun."
  - path: PI_PORT_TODO.md
    description: "Documents the porting progress of pi's prompt box and scrolling features into fff, listing implemented and remaining work."
  - path: README.md
    description: "Describes fff as a terminal AI coding assistant, its features, installation steps, and requirements."
  - path: src/agent-runner.ts
    description: "Orchestrates the agent loop by executing LLM calls, tool invocations, and managing conversation state."
  - path: src/app.tsx
    description: "Serves as the main React component for the TUI, managing state for messages, streaming, and input."
  - path: src/clipboard.ts
    description: "Provides a cross-platform function to copy text to the system clipboard."
  - path: src/config.ts
    description: "Exports color constants and system prompt configuration for the fff UI."
  - path: src/conversation.ts
    description: "Prunes conversation messages to a maximum length, preserving the system message."
  - path: src/index.tsx
    description: "Entry point that dispatches to the indexer subcommand or starts the interactive TUI."
  - path: src/indexer.ts
    description: "Walks the project and generates a codebase-index.yaml with one-line summaries of source files."
  - path: src/input-editor.ts
    description: "Provides grapheme-aware text editing functions like insert, backspace, and word navigation."
  - path: src/input-panel.tsx
    description: "Renders the multi-line input panel with cursor positioning and visual line wrapping."
  - path: src/kill-ring.ts
    description: "Implements an Emacs-style kill ring for managing killed text entries with yank support."
  - path: src/llm.ts
    description: "Configures the OpenAI client and exports model constants, message types, and tool description generation."
  - path: src/logger.ts
    description: "Provides a lightweight, non-throwing file logger for session traces."
  - path: src/message-format.ts
    description: "Contains utilities for wrapping text, parsing code blocks, and formatting tool invocations."
  - path: src/message-line.tsx
    description: "Renders a single message line with syntax highlighting for tool calls and code blocks."
  - path: src/message-viewport.tsx
    description: "Renders the scrollable viewport of messages with streaming preview support."
  - path: src/paste.ts
    description: "Handles bracketed-paste normalization and large-paste collapsing into markers for editor readability."
  - path: src/pi-prompt-utils.ts
    description: "Provides visual line wrapping for input text based on terminal width, returning an array of visual line objects."
  - path: src/process-registry.ts
    description: "Manages a central registry of child processes to ensure they are killed on exit, preventing orphaned processes."
  - path: src/text-segmentation.ts
    description: "Offers grapheme- and word-aware text segmentation utilities using Intl.Segmenter for cursor navigation."
  - path: src/tools-registry.ts
    description: "Registers available tools (read, list, edit, overwrite, run command) in a typed registry for tool invocation."
  - path: src/tools.ts
    description: "Implements file system and command execution tools with path resolution, timeout, and output limits."
  - path: src/types.ts
    description: "Defines TypeScript types for application status, segments, input history, and agent state refs."
  - path: src/undo-stack.ts
    description: "Implements a generic undo stack that stores deep-cloned state snapshots for undo functionality."
  - path: src/use-alternate-screen.ts
    description: "Provides a no-op hook for alternate screen lifecycle, now managed centrally in the entry point."
  - path: src/use-app-input.ts
    description: "Handles keyboard input for the application, integrating editing, navigation, and tool invocation."
  - path: src/viewport.ts
    description: "Builds a viewport model for scrolling through messages, accounting for terminal dimensions and streaming state."
  - path: src/word-navigation.ts
    description: "Provides word-boundary navigation functions (forward/backward) using the shared word segmenter."
  - path: tsconfig.json
    description: "Configures TypeScript compiler options for a Bun-based project with strict mode and bundler module resolution."
